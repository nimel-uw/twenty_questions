//Author: Nathaniel Imel
//Date: 10/3/20
#include "node.h"

// must have no daughters, works for Qs and leaves
Node* ::Node::insert( Node* leaf, Node* root ) {
   if ( root ) {
      if ( leaf ) {
        //what goes here? 
        return lowerNode;
      }
   }
   return nullptr;
}
